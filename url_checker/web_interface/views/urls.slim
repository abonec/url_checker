table
  thead
    tr
      th Url
      th State
      th Uptime
  tbody
    - @workers.sort_by(&:formatted_uptime).each do |worker|
      tr
        td
          a href=worker.url target='_blank' = worker.url
        td = worker.status
        td = worker.formatted_uptime